server {
    listen 80;
    server_name localhost;  # Replace with your actual domain name or IP address.

    location / {
        root /app;  # Replace with the actual path to your files.
    }

    # You can add additional configuration here as needed.
}
